/* Write a program which creates properties for the following data types and associated validations
Decide on the best property implementation to use ie function or auto properties.
Verify that the program is working as expected by setting and getting values and displaying these on the console

1)  int height      (public read/public write)  - any value
2)  int width       (public read/public write)  - any value
3)  int perimeter   (public read only)          - any value   (result of width * height)
4)  int pi          (public read/public write   - write only 3.14 allowed
5)  string password (public read/private write) - write only strings that '%', '&' or '$' characters
6)  double currency (public write only)         - only when 0 <= value <= 50.2
*/
