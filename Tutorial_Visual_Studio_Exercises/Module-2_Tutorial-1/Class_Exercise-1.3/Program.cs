/*
 * Create a Class called MathsSquare with 2 void methods each called square, one takes a double by value and 
 * the other takes a double by reference.

 * Each method performs a square function (ie it multiplies the number supplied by itself)
 * 
 * Prompt the user to enter a number to square
 * 
 * First call the square method by value and print the result on the console
 * Then call the square method by reference and print the result on the console
 */

using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Class_Exercise_1._3
{
    class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
